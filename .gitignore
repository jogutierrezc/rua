node_modules
dist
dist-ssr
*.tsbuildinfo
*.local
# TODO lo que empiece por .env queda fuera, salvo el ejemplo. Enumerar
# nombres concretos falló: un `.env.respaldo` temporal se coló en un commit.
.env*
!.env.example

# Repo de skills clonado (no forma parte de la app)
skills/

# Supabase CLI
supabase/.branches
supabase/.temp
.DS_Store
*.log
